# fs-mcp-playbooks
Persistent metadata definitions, operational skills, and frameworks designed to scale complex analytical capabilities within Fullstory MCP.
├── skills/
│   └── retail_ab_test_analyzer.md   # The conversational logic & instructions
└── config/
    └── org_metadata.json            # State file containing Org IDs, page mappings, & cached metric IDs
